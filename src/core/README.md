# Day 1 launch notes

- Open from local static server to avoid browser fetch restrictions.
- Core modules are added in later days (`demandModel.js`, `costModel.js`).
- Current goal: state bootstrap and day tick.
